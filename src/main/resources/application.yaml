spring:
  mail:
    host: "smtp.gmail.com"
    port: 587
    username: "<email-for-smtp>"
    password: "<password-for-smtp>" #generated from https://myaccount.google.com/apppasswords
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
